/*
 * Form validation
 */

// Username
xit('return error if username is empty', () => {
  expect(false).toBeTruthy();
});

xit('return error if username less than 5 character', () => {
  expect(false).toBeTruthy();
});

xit('return error if username is not alphanumeric', () => {
  expect(false).toBeTruthy();
});

xit('return error if username is more than 30 character', () => {
  expect(false).toBeTruthy();
});

// Password
xit('return error if password is empty', () => {
  expect(false).toBeTruthy();
});

xit('return error if password is less than 7 character', () => {
  expect(false).toBeTruthy();
});

xit('return error if password is more than 30 character', () => {
  expect(false).toBeTruthy();
});

xit('return error if password has space', () => {
  expect(false).toBeTruthy();
});

xit('return error if password doesnt contain special character', () => {
  expect(false).toBeTruthy();
});

xit('return error if password doesnt contain uppercase', () => {
  expect(false).toBeTruthy();
});

xit('return error if password repeat doesnt match', () => {
  expect(false).toBeTruthy();
});

// Name
xit('return error if name is empty', () => {
  expect(false).toBeTruthy();
});

xit('return error if name is more than 50 character', () => {
  expect(false).toBeTruthy();
});

// Email
xit('return error if email is empty', () => {
  expect(false).toBeTruthy();
});

xit('return error if email is not valid', () => {
  expect(false).toBeTruthy();
});
